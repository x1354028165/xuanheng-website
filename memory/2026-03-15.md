# 2026-03-15 日志

## 旭衡电子官网重构项目 — 今日进展

### 会话状态
- 时间范围：2026-03-14 深夜 ~ 2026-03-15 01:51 UTC
- 当前阶段：Phase 0 完成，Phase 1 待启动（等主人下令）

### Phase 0 完成状态（commit: 67fc882）
- Next.js 16 + TypeScript + Tailwind v4 + App Router 初始化
- Strapi v5.39.0 + SQLite（注意：装了 v5，不是文档里写的 v4）
- next-intl v4（8语言路由底座）
- Header（透明浮层/滚动变色/汉堡菜单）+ Footer（4列深色）
- shadcn/ui v4（7个组件）
- DeepSeek 翻译服务（translateText 函数，已含 Markdown 包裹清洗 + URL 保护 + CRITICAL 指令）

### 重大技术决策
- **Strapi v5 vs v4**：Phase 0 Agent 装了 v5.39.0，与文档原设 v4 不同
  - v5 破坏性变更一：使用 documentId（非 id）作为唯一标识
  - v5 破坏性变更二：API 响应已扁平化，无 .attributes 层
  - v5 使用 Document Service API（strapi.documents()），禁止旧 entityService
- **DeepSeek API Key**：sk-e952bf0551ca4e2b843035c6aa02cecc（存于 .secrets + cms/.env）
- **数据库**：开发用 SQLite，生产切 PostgreSQL
- **翻译方案**：DeepSeek API（国内可访问 + 极低成本 ¥10/3年）
- **zh-TW**：不存数据库，fetchStrapi 内拦截 locale=zh-TW 改为 zh-CN + OpenCC 转换

### 关键文件路径
- 项目仓库：https://github.com/x1354028165/xuanheng-website.git
- 工作目录：/home/ec2-user/.openclaw/workspace
- 前端：frontend/（Next.js）
- CMS：cms/（Strapi v5）
- 文档：docs/（9份HTML规范文档）
- 环境变量模板：frontend/.env.example + cms/.env.example
- 密钥存档：.secrets（gitignore）

### 今日大规模文档排雷（累计12+轮，40+处修复）

**已修复的关键断层（按类别）：**

数据层：
- product_relations 表外键修正（product_id + solution_id）
- 所有多语言表补 is_translation_locked 字段
- SEO Shared Component（metaTitle/metaDescription/keywords/ogImage）挂载到所有 Collection Type
- Strapi v5 防幻觉：documentId + 无 attributes + Document Service API

前端规范：
- fetchStrapi：zh-TW 拦截 + cacheTags + fetchWithFallback + 写操作铁律
- getStrapiMedia()：相对路径补全 Strapi 域名
- OpenCC 单例 + URL 保护（提取URL→转换→还原）
- next-intl v4：setRequestLocale（非 v3 的 unstable 版）+ 异步 RSC 也必须调用
- 路由铁律：禁止 next/link，从 @/i18n/routing 导入
- RSC 边界：page.tsx 永远是 Server Component
- Tailwind Typography（prose 类）for Markdown
- Image remotePatterns（CDN + localhost:1337）
- generateStaticParams 双层遍历（locale + slug）
- dynamicParams = true + revalidate = 3600
- 日期格式化：禁止 toLocaleDateString，用 next-intl useFormatter
- hreflang alternates 铁律
- 空数据 notFound() 防御
- Draft Mode 实现
- 多语言 Fallback 链（目标语言→en-US→zh-CN）
- Route Handler 语法铁律（NextRequest/NextResponse）
- Zod 表单 i18n 动态构建
- Markdown 内图片渲染（ReactMarkdown img 重写）
- SEO 分页铁律（SSR + URL SearchParams）
- ISR 精准刷新（revalidateTag，禁止 revalidatePath('/')）
- 防幽灵缓存（监听4种事件：publish/update/unpublish/delete）
- Strapi v5 Webhook Payload（body.model + body.documentId）

CMS/后端：
- Toast UI Editor → 灵活 Markdown 插件描述
- 文件下载铁律（禁止直链，走 /api/download/{id}）
- Strapi CSP（img-src/media-src 加入 OSS 域名）
- 翻译防雪崩（for...of 串行 + 1秒延迟，禁止 Promise.all）
- v5 Document Service API 示例
- 构建并发 Rate Limit 白名单
- populate 铁律（开发用 *，生产用精确字段）
- 分页铁律（pagination[limit]=-1）
- 表单防刷（Cloudflare Turnstile + IP Rate Limit 5次/小时）
- afterPublish 异步执行（禁止 await 阻塞，用 .catch(console.error)）

### 当前代码状态（latest commit: b762fe2 附近）
- Phase 0 代码干净，含 layout.tsx（双层正确结构）、middleware.ts（已修复匹配规则）
- translate.ts：已含 Markdown 包裹清洗 + URL 保护 Prompt
- next.config.ts：remotePatterns 含 CDN + localhost:1337

### 待执行（等主人下令）
- P0-S：Strapi Collection Type 建模 + 种子数据（Phase 1 前台的 API 依赖）
- Phase 1：24个前台页面全部开发

### 主人操作偏好
- 不说"开始"不启动任何 Agent
- 之前一次我越权启动了 Phase 1，已被叫停并认错
- 主人喜欢先排雷再开工，需耐心等待
- 中文交流

### 重要约束
- 所有 Agent 启动必须等主人显式说"开始"
- Phase 完成后立即汇报，不等主人问
- 发现代码/文档矛盾必须先验证再接受，误报一律驳回
